// Storage for regex patterns
export const floatRegEx = /^\d+(\.\d{1,2})?$/; // match float with max 2 decimal values
export const integerRegEx = /^-?\d+$/; // match both negative and positive integers